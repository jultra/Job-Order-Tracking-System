%head
  :css
    #myUL3 {
        list-style-type: none;
        padding: 0;
        margin: 0;
      }

    #myUL3 li {
      border: 1px solid #ddd;
      margin-top: -1px; /* Prevent double borders */
      background-color: #f6f6f6;
      padding: 12px;
      text-decoration: none;
      font-size: 18px;
      color: black;
      display: block
    }

    #myUL3 li:hover:not(.header) {
      background-color: #eee;
    }
%body
  - if @results.empty?
    %ul#myUL3
      %li
        %a{:href => "#"} Result not found
  - else
    %ul#myUL3
      - @results.each do |post|
        %li{:id => post.id}= post.fname + " " +post.mname + " " + post.lname
